# Copy to deploy.polimi.env (gitignored) and fill in.
# Use single quotes if the password contains ! or spaces:
#   FTP_PASS='your-password-here'

FTP_HOST=131.175.186.58
FTP_PORT=2121
FTP_USER=mimic
FTP_PASS=

# Document root for HTTPS (confirm with ICT)
FTP_REMOTE_DIR=htdocs-SSL

# Optional: set to 1 to re-upload when local file is newer (same size still skipped vs --ignore-time default)
# FTP_MIRROR_RESPECT_TIME=1
