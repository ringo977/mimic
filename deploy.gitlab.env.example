# Copy to deploy.gitlab.env (gitignored) and fill in your Personal Access Token.
# Create token at: gitlab.polimi.it → Preferences → Access Tokens
# Required scopes: read_repository, write_repository

GITLAB_TOKEN=
GITLAB_REPO=https://gitlab.polimi.it/DEIB/mimic.git
GITLAB_SUBFOLDER=mimic-website
