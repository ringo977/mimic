/**
 * Must match `basePath` in next.config.js (set at build via NEXT_PUBLIC_BASE_PATH).
 * GitHub Pages: /mimic — Polimi root: ''.
 */
export const siteBasePath = process.env.NEXT_PUBLIC_BASE_PATH ?? '';
